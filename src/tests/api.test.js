describe('Performing all api tests', () => {
  it('Attempting to sign-in', done => {
    done()
  })

  it('Attempting to sign-up', done => {
    done()
  })

  it('Fetching assets', done => {
    done()
  })

  it('Fetching transactions', done => {
    done()
  })

  it('Fetching wallets', done => {
    done()
  })
})
